# MindFlow — Python FastAPI Backend
# semantic-kernel is pinned at 1.41.3 (confirmed stable at project start).
# All other packages use >= to let pip resolve compatible versions
# that satisfy semantic-kernel's transitive dependency requirements.

semantic-kernel==1.41.3
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
azure-cosmos>=4.7.0
azure-search-documents>=11.6.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
python-dotenv>=1.0.1
